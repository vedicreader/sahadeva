__pycache__/
*.py[cod]
.pytest_cache/
*.egg-info/
.venv/
venv/

# pipeline outputs — regenerate with sd-fetch / sd-build / sd-train
data/
out/
ckpt/
# downloaded weights (vagdhenu ~3GB) — public on HF, fetch rather than store
models/
*.wav
!tests/fixtures/*.wav
manifest*.jsonl
